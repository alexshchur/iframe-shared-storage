{
  "name": "iframe-storage",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=iife --global-name=IframeStorage --outfile=dist/browser.js",
    "build:watch": "esbuild src/index.ts --watch --bundle --format=iife --global-name=IframeStorage --outfile=dist/browser.js",
    "serve:client": "node scripts/server.js --file client.html --port 5100 --host 0.0.0.0",
    "serve:hub": "node scripts/server.js --file hub.html --port 5101 --host 0.0.0.0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "postmsg-rpc": "^2.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "esbuild": "^0.24.0"
  }
}
